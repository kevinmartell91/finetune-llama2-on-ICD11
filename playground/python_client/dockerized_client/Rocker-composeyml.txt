services:
  app:
    build: .
    container_name: python-server
    command: uvicorn answer-sample-questions:app --host 0.0.0.0 --port 80 --reload
    ports:
      - 80:80
    volumes:
      - .:/code